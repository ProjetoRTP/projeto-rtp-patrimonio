tags: [Subsetores]
summary: Atualiza subsetor
security: [{Bearer: []}]

parameters:
  - in: path
    name: url_id
    type: integer
    required: true
  - in: body
    name: dados
    schema:
      type: object
      properties:
        nome: {type: string}
        setor_id: {type: integer}
responses:
  200: {description: Sucesso}
  201: {description: Criado}
